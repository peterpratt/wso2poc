openapi: 3.0.1
info:
  title: AddMessageToQueueAPI
  description: API Definition of AddMessageToQueueAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/addMessage
paths:
  /toQueue:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
